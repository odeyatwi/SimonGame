import React, {FunctionComponent} from "react";
import {View} from "react-native";
import {observer} from "mobx-react";

const ResultsScreen: FunctionComponent = () => {
    return <View>

    </View>
}

export default observer(ResultsScreen)
